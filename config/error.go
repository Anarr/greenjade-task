package config

const(
	ErrLevelCanNotEmpty = "level can not be empty"
	ErrDimensionSize = "dimension size must be less %d"
	ErrDimensionLen = "each dimension must the same len"
	ErrDimensionElement = "dimension elements value must be 0, 1 and 2"
	ErrIdMustNumeric = ":id must be numeric"
	ErrNotExistsLevel = "not exists level"
	ErrDecodeErr = "could not handle user request"
)